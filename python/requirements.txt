# ============================================================
# Robotrend IA — SofaScore (RapidAPI) Python client
# ============================================================
# Instale tudo de uma vez:
#   pip install -r requirements.txt
#
# Ou rode o helper que faz isso automaticamente:
#   python install.py
# ============================================================

requests>=2.31.0
python-dotenv>=1.0.1
